# Mounted into the UI container by docker-compose.prod.yml. Applies to every
# location in the server block except those that say `auth_basic off`.
auth_basic           "fab dispatch";
auth_basic_user_file /etc/nginx/auth/htpasswd;
