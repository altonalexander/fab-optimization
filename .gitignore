# build outputs
dispatch/fabdisp
dispatch/fab_scenario
dispatch/hsms_sim
dispatch/amhs_adapter
dispatch/fabtest
dispatch/build/
# No trailing slash: in a git worktree this is usually a symlink to the main
# checkout's node_modules, and a trailing slash matches only a real directory.
dispatch/ui/node_modules
dispatch/ui/dist/

# python
__pycache__/
*.pyc
.venv/
venv/

# baseline run artifacts
wandb/
baselines/pyscfabsim/debug_data/
baselines/pyscfabsim/docs/

# bench outputs are committed deliberately; scratch is not
bench/scratch/

# windows / editor junk
*:Zone.Identifier
.DS_Store

# original unsorted drop, kept on disk as a safety net
unorganized/
dispatch/build-ortools/

# local dev run state
.dev-run/

# claude code worktrees and session state
.claude/

# warm-up snapshots: regenerable, and large
bench/snapshots/

# sim playback control: runtime state written by the feed and the API
bench/.sim_control.json
